### Answer

```c++
#include <iostream>

int main(){
    // Declare a variable of type `int` and initialize it to the value 10.
    int number1 = 10;
    // Declare a variable of type `float` and initialize it to the value 3.14.
    float pie = 3.14;
    // Declare a variable of type `char` and initialize it to the character 'A'
    char ch = 'A';
    // Declare a variable of type `bool` and initialize it to the value true
    bool status = true;
    // Print the values of the variables you declared in exercises 1-4
    std::cout << "Integer: " << number1 << ", Float:" << pie << ", Character:" << ch << ", Boolean:" << status << std::endl;
}
```